#include <stdio.h>
#include <stdlib.h>

int main() {
	/* No tengo idea de qué poner acá hace 1 semana que estoy intentando
	hacer andar esto con RegEx y no  me salía porque me estaba comiendo un corchete
	o alguna boludez así y ni AWK ni bash se dignaban a avisarme, además de que usan
	el estándar de POSIX para las RegEx y es una cagada tremenda o bueno no es una cagada
	pero está desactualizado en comparación con el PCRE.

	*/
	int var = 42;
	printf("Meaning of life: %d", var); // Prints the meaning of life
	printf("That's all folks!");

	int arr[5] = { 1 , 2, 3 , 4, 5 }; // Super array
	//Index variable 
	int i; // Index variable
	//Index variable 

	for (i = 0; i < 10; i++) {
		//	Prints something
		printf("Super array value: %d", arr[i]);
	}

	// :)!
	/* No estoy feliz. */
	/* asdfd
	asd */
	return 0;
}